--dofile("./lua/player_input.lua")
--dofile("./lua/settings.lua")
function idle() 
end

function init()
    soloscuro.load_window("main") -- main title screen
    -- After init the game loop runs
end

