-- Debug/Developer Options
function game_settings()
    -- Movement Options
    soloscuro.set_player_frame_delay(7) -- set how many delay ticks between frame changes of a player sprite. (default: 7)
    soloscuro.set_player_move(10) -- set how many ticks to move one slot. (default: 10)
    --soloscuro.set_quiet(1) -- uncomment to place into quiet mode
end
